{
  "name": "webhook-api",
  "version": "1.0.0",
  "description": "Node.js Express API for handling shipment webhooks and sending email notifications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required for Node.js API'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "webhook",
    "express",
    "resend",
    "email",
    "shipment",
    "notifications"
  ],
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "morgan": "^1.10.1",
    "resend": "^3.2.0"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
